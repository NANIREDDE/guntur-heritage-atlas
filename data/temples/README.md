# Temple Data

One file per temple. Use the schema in `docs/data-model.md`.

Naming convention: `<temple_id>.json`.

Example: `GHA-TEM-0001.json`.

Only publish historical claims when their evidence is recorded in the `sources` section. Research notes can remain in `status: research` until reviewed.
