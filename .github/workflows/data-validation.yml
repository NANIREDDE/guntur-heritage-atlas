name: Heritage data validation

on:
  push:
  pull_request:
    branches:
      - main
      - build/atlas-next

jobs:
  validate-data:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Validate research records
        run: python3 scripts/validate_data.py
